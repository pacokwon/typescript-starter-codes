# Sample webpack + React project in TypeScript

to install dependencies:
```bash
$ npm install
```

to open up a development server:
```bash
$ npm run dev
```

check the `sample` branch for sample implementations for `Counter` and `ComplicatedCounter`
